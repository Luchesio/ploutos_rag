# ── Web framework ─────────────────────────────────────────────────────────
fastapi
uvicorn[standard]

# ── Pydantic / settings ───────────────────────────────────────────────────
pydantic
pydantic-settings
pydantic[email]

# ── .env loading ──────────────────────────────────────────────────────────
python-dotenv

# ── Gemini ────────────────────────────────────────────────────────────────
google-genai

# ── Vector store ──────────────────────────────────────────────────────────
# Qdrant Cloud replaces ChromaDB. ChromaDB requires hnswlib which needs
# libgomp.so.1 (OpenMP) — a native system library absent from Vercel/Lambda.
# Qdrant is fully hosted so there are no native dependencies.
qdrant-client

# ── Data processing ───────────────────────────────────────────────────────
pandas
openpyxl

# ── Retry logic ───────────────────────────────────────────────────────────
tenacity

# ── MongoDB async driver ──────────────────────────────────────────────────
motor

# ── Authentication ────────────────────────────────────────────────────────
bcrypt
python-jose[cryptography]