# ── Web framework ─────────────────────────────────────────────────────────
fastapi
uvicorn[standard]

# ── Pydantic / settings ───────────────────────────────────────────────────
pydantic
pydantic-settings
pydantic[email]

# ── .env loading ──────────────────────────────────────────────────────────
python-dotenv

# ── Gemini ────────────────────────────────────────────────────────────────
google-genai

# ── Vector store ──────────────────────────────────────────────────────────
# Pinned to 0.4.24 — last pure-Python release before Rust bindings were
# introduced in 0.5.0. Newer versions crash on serverless (Vercel/Lambda)
# with: AttributeError: 'RustBindingsAPI' object has no attribute 'bindings'
chromadb==0.4.24

# ── Data processing ───────────────────────────────────────────────────────
pandas
openpyxl

# ── Retry logic ───────────────────────────────────────────────────────────
tenacity

# ── MongoDB async driver ──────────────────────────────────────────────────
motor

# ── Authentication ────────────────────────────────────────────────────────
bcrypt
python-jose[cryptography]